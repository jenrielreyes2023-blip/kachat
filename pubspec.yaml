name: kachat
description: "KatsKlub Chat App"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  tencent_cloud_chat_uikit: ^5.0.1+4
  tencent_cloud_chat_sdk: ^8.6.7019+2
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
